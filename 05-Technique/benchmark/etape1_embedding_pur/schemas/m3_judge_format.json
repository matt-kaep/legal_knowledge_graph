{
  "type": "object",
  "properties": {
    "label": {
      "type": "string",
      "enum": ["n2", "n1", "n0"],
      "description": "n2 = pertinent (fonde directement la reponse), n1 = proche (meme theme, contexte utile mais non central), n0 = non pertinent."
    },
    "raison": {
      "type": "string",
      "description": "Justification en une seule phrase, en francais."
    }
  },
  "required": ["label", "raison"],
  "additionalProperties": false
}
